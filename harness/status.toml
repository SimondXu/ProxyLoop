schema_version = 1
product_phase_state = "idle"
active_product_phase = ""
active_contract = ""
next_phase_authorized = false
integration_baseline = "main"
updated_at = "2026-09-25"

[boundaries]
summary = "Phase 07 (Portfolio Hardening, build-plan PR-16 and PR-17, contract harness/build/phase-07-portfolio-hardening.md) is complete within the authorized limits: the credential-free 07A demo runs the whole journey in the product's order (intake, card, Case, Standing Proposal and scripted Judge, one Assistant Message, approval, one execution, verified receipt), make ops-report summarizes the gates and measurements offline, and the final reports are docs/ml-evidence.md, docs/limitations.md, the architecture reconciliation and the README. The gate passed on PR-17's head: make preflight (67 gated skips = pin), postgres-check 39, phase05a-check 73, phase06b1-check 56 (harness/log/phase-07-portfolio-hardening.md). This closes the audit-remediation and build-to-complete programme (decisions 16-21; harness/context/audit-remediation-status.md). Phase 03C stays GO_DISTILLED on the trained prompt path only: through the product path the local opt-in adapter delivers 0/240 lines; nothing is promoted. Not done (docs/limitations.md): production serving, real-model load, p95, capacity, concurrency, OOM and automatic fallback; production exactly-once effects, monitoring and readiness; deployment, hosting and release; Phase 06B2 and every real channel and credential; V0, frontier-as-Fast and a second-family Judge (not measured, budget); a model Judge; further training, data expansion, reruns or promotion; narrow contracts 1.2 (PR-15, dropped by decision 21); the build-plan Do-not-do items. No phase is active; the next one is a new user decision."
inactive = [
  "Phase 03A1 continuation or r6/r7",
  "any model training, data expansion, or promotion; Phase 03A1/03B artifact rewrites (Stage 0 writes only new phase-03c artifacts)",
  "PostgreSQL work beyond the completed Phase 06B1 and Phase 07A local demo state",
  "promoted or production model serving, real-model load, OOM, production p95, or automatic fallback work (local opt-in serving of the 03C adapter is authorized by decision 18)",
  "Temporal work beyond the completed Phase 06B1 behavior and Phase 07A local recovery demonstration",
  "real tools, Providers, email, MCP, SMS, or external network channels",
  "authentication, real channels, voice, or production UI",
  "deployment or release",
]

[history]
phase_index = "PLANS.md"
legacy_execution_log = "harness/build-log.md"
current_logs = "harness/log/"
