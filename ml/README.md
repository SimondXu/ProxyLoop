# ML workspace

This independent environment will contain data curation, training, evaluation, and promoted-serving configuration. Model weights, datasets, and experiment artifacts are excluded from Git.
