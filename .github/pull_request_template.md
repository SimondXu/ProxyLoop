<!-- Title must be "<TASK-ID>: <title>", e.g. "S0-SYS-03: event log, fold, evidence-check". CI rejects other titles. -->

## Task
- **ID / title:** <!-- S0-SYS-03 — … -->
- **Lane:** CON / SYS / MOD / ROOT
- **PLAN.md task block:** <!-- link to the heading -->
- **Size budget vs actual:** <!-- M ≤ 700 / actual N changed lines (excl. tests) -->

## What became real
<!-- One or two sentences: what can now happen with real models or real users that could not before. "Nothing yet; provisional until <root-run task>" is a valid answer. -->

## Evidence (bundle ids / artefacts / provenance)
| Kind | Id / path | Check |
|---|---|---|
| bundle | `evidence/<stage>/<run_id>/` | `make evidence-check RUN=… --claim` → pass/fail |
| artefact | `docs/results/<…>.json` / `docs/decisions/data/<…>.json` | generated by `<command>` |
| adapter | `<served name>`; shard sha256 list in the card | liveness Δ logprob = … |

<!-- Leave rows empty and say "provisional" if no real run is cited. -->

## Reality statement
| Role / component | Adapter kind in this PR | Where |
|---|---|---|
| Fast (user / cp) | real_http / recorded_replay / test_fake / baseline | <!-- paths --> |
| Slow | | |
| World (Ear / Mouth / SimUser) | | |
| Human | | |

- [ ] Nothing under `src/` imports `tests/support` (import-linter green).
- [ ] No fallback path; a dead endpoint raises `LLMUnavailable` and aborts.
- [ ] Model-touching criteria are either proven by a cited bundle or marked "needs root run".

## Checks run (paste tails; never claim unrun checks)
| Command | Result |
|---|---|
| `make check` | |
| <!-- task verification --> | |
| Root-run (L/G/U) | done by root: … / pending: … |

## Ownership and contract
- [ ] The diff is inside the task's owned paths (`git diff --name-only origin/main...HEAD`).
- [ ] Contract (`src/proxyloop/contract/**`, `tests/contract/**`, `tests/golden/**`) untouched, **or** ADR `docs/decisions/NNNN-…` + CON task id: ____, and `make pull-through MODE=…` result: ____.
- Dependency added to `[dependency-groups]`: <!-- none / name==version -->

## Invariants touched (NORTH_STAR I1–I11)
<!-- e.g. "I4: view_cp unchanged; counterfactual test green. I8: evidence-check gains the X rule." -->

## Reviewer (fresh-context) — root pastes the reviewer's mandatory fields here
<!-- 1 make check · 2 stubbed/dead-endpoint · 3 defects or cases tried · 4 invariants · 5 owned paths/contract · 6 second path/fallback/TTFS/process doc · 7 anti-absorption · 8 reality · 9 acceptance matrix · 10 recommendation -->

## Root decision
<!-- merge / fix / block; which acceptance criteria stay provisional and which root-run task closes them -->
